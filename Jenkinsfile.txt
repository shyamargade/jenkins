node {
  stage('SCM') {
    checkout scm
  }
  stage('SonarQubeAnalysis') {
    def mvn = tool 'M2_HOME';
    withSonarQubeEnv() {
      sh "${mvn}/bin/mvn clean verify sonar:sonar -Dsonar.projectKey=demoproject"
    }
  }
}